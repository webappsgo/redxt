# Project SPEC (alternate location)

See `/CLAUDE.md` at the project root — that is the canonical loader
for this project. This file exists only to satisfy tools that look
in `.claude/CLAUDE.md` instead of the root; keep it in sync with the
root file whenever the root loader changes.
